{
  "name": "edgeapps",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm --filter gh-proxy run build",
    "release": "pnpm --filter gh-proxy release",
    "clean": "pnpm --filter gh-proxy run clean"
  }
}
