{
  "name": "edgeapps",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm --filter gh-proxy run build",
    "build:watch": "pnpm --filter gh-proxy run build:watch",
    "release": "pnpm --filter gh-proxy run release",
    "clean": "pnpm --filter gh-proxy run clean"
  }
}
