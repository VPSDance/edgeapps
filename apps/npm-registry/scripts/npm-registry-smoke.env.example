# npm-registry smoke test env

# hosts
NPM_REGISTRY_HOSTS=https://npm-registry.pages.dev

# base token (required): used for read-path checks and as write fallback
NPM_REGISTRY_TOKEN=token_read_or_rw

# required: package name for read-path checks (e.g. lodash)
NPM_REGISTRY_READ_TEST_PACKAGE=lodash

# admin basic test (optional)
# format: username:token
NPM_REGISTRY_ADMIN_BASIC=admin:token_read_or_rw

# write-path tests (publish + dist-tag + unpublish)
# required: package name used by write-path tests
# examples:
# - scoped: @your-scope/smoke
# - unscoped: your-team-smoke
NPM_REGISTRY_WRITE_TEST_PACKAGE=@your-scope/smoke
# optional override for write-path; defaults to NPM_REGISTRY_TOKEN
# NPM_REGISTRY_WRITE_TOKEN=token_rw
